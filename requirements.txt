# Pinned to the versions resolvable on PyPI as of 2026-09-10 (python:3.12).
# Bump deliberately: marker-pdf drags torch + model code, rq/redis must agree.
marker-pdf[full]==2.0.0
psutil==7.2.2
rq==2.12.0
redis==8.1.0
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
